# Configurações do GitHub para Push Notifications
# Copie este arquivo para .env (localmente) ou configure no Netlify Dashboard

# Token de acesso do GitHub (obrigatório)
# Como obter: https://github.com/settings/tokens/new
# Permissões necessárias: Contents (Read and write)
PUSH_GITHUB_TOKEN=seu_token_aqui

# Dono do repositório (opcional, padrão: gabrielfavera07)
GITHUB_OWNER=gabrielfavera07

# Nome do repositório (opcional, padrão: acaiecia)
GITHUB_REPO=acaiecia

# Branch do repositório (opcional, padrão: main)
GITHUB_BRANCH=main

# Caminho do arquivo JSON (opcional, padrão: products_with_prices.json)
GITHUB_FILE_PATH=products_with_prices.json
